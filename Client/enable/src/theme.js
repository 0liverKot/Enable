import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#ebe2e1'
        },
        secondary: {
            main: '#ebdbda'
        }
    }
})

export default theme